# Backend Dependencies — Walkie Talkie Inventory Agent
# Install: pip install -r requirements.txt

# -- Web Framework --
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0

# -- Database (SQLite async) --
aiosqlite==0.20.0

# -- Speech-to-Text (Faster-Whisper, offline) --
faster-whisper==1.1.0
numpy>=1.24.0          # Used in speech.py for audio array conversion

# -- Text-to-Speech (Microsoft Edge TTS) --
edge-tts==6.1.12

# -- HTTP client (for Direct Line API) --
httpx==0.27.0

# -- Multipart file uploads --
python-multipart==0.0.12

# -- Environment variable loading --
python-dotenv==1.0.1

# -- Required by faster-whisper for model downloads --
requests>=2.31.0
